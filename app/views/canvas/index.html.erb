<h3 class="alt">Your bookshelf</h3>

<div class="bookshelf">
	<div id="books">
		<% @uploads.each do |upload| %>
			<div class="book-thumb">
				<%= link_to image_tag(thumbnail_url(upload), :height => 105), canvas_viewer_path(upload.id) %>
			</div>
		<% end %>
	</div>
</div>