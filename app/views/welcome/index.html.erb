<h3 class="alt">Public bookshelf</h3>

<div class="bookshelf">
	<div id="public-books">
		<% @uploads.each do |upload| %>
			<div class="book-thumb">
				<%= link_to image_tag(thumbnail_url(upload), :height => 105), "/simpleviewer/#{upload.id}/1" %>
			</div>
		<% end %>
	</div>
</div>